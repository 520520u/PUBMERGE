# Synthetic fixtures

These `.jwlibrary` files contain invented notes and tags only. They do not include personal study data.

Regenerate them:

```bash
cd Packages/PubMergeCore
swift run GenerateFixtures
```
